CREATE TABLE IF NOT EXISTS t_login_users (
userid TEXT NULL,
account TEXT DEFAULT NULL,
passwd TEXT DEFAULT NULL,
logindate TEXT DEFAULT NULL,
soundenable TEXT DEFAULT 1,
shakeenable TEXT DEFAULT 1,
soundname TEXT DEFAULT 'in.caf',
PRIMARY KEY (userid)
)|
CREATE TABLE IF NOT EXISTS t_cnns (
title TEXT DEFAULT NULL,
scribed TEXT DEFAULT NULL,
hot TEXT DEFAULT NULL,
PRIMARY KEY (title)
)|
CREATE TABLE IF NOT EXISTS t_news_titles (
id TEXT NULL,
boardid TEXT DEFAULT NULL,
digest TEXT DEFAULT NULL,
docid TEXT DEFAULT NULL,
imgType TEXT DEFAULT NULL,
imgsrc TEXT DEFAULT NULL,
lmodify TEXT DEFAULT NULL,
ltitle TEXT DEFAULT NULL,
postid TEXT DEFAULT NULL,
priority TEXT DEFAULT NULL,
ptime TEXT DEFAULT NULL,
replyCount TEXT DEFAULT NULL,
skipID TEXT DEFAULT NULL,
skipType TEXT DEFAULT NULL,
source TEXT DEFAULT NULL,
specialID TEXT DEFAULT NULL,
subtitle TEXT DEFAULT NULL,
title TEXT DEFAULT NULL,
url TEXT DEFAULT NULL,
votecount TEXT DEFAULT NULL,
ads TEXT DEFAULT NULL,
hasAD TEXT DEFAULT NULL,
imgextra TEXT DEFAULT NULL,
savetime TEXT DEFAULT NULL,
navitype TEXT DEFAULT NULL,
PRIMARY KEY (id)
)|
CREATE TABLE IF NOT EXISTS t_news_read (
boardid TEXT DEFAULT NULL,
digest TEXT DEFAULT NULL,
docid TEXT DEFAULT NULL,
ltitle TEXT DEFAULT NULL,
subtitle TEXT DEFAULT NULL,
title TEXT DEFAULT NULL,
rtime TEXT DEFAULT NULL,
PRIMARY KEY (boardid)
)